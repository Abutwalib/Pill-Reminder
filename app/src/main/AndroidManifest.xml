<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.piremedtime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Verify2"></activity>
        <activity android:name=".Verify" />
        <activity android:name=".NotificationTest" />
        <activity android:name=".ListActivity" />
        <activity android:name=".Emergency" />
        <activity android:name=".DoctorsOptions" />
        <activity android:name=".PatientOptions" />
        <activity android:name=".Profile" />
        <activity android:name=".ReminderAdd" />
        <activity android:name=".Patients" />
        <activity android:name=".Doctors" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>